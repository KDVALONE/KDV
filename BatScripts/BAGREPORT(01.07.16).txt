UVNCINST v0.88

 ок -- При установке не берет данные из log файла в дериктории, просто не выполняет их.(не видит log файла ?).
 ОК-- Изза этого устанавливается по умолчанию, не устанавливая службу uvnc_service 
 ок Так же изза этого нельзя перекинуть в корневую директорию файл .ini, так как путь указывается по умолчанию. (решаемо если в батнике указать оригинальный путь.)

	Ок-- BG info:
		*-Запускается BGinfo.exe отображаются все данные.
	 	*-Если скриптом заставить запускать заранее подготовленный bginfo.bgi то команда /Timer:0 не работает
	-- Скрипт:
		*-на вызове CALL :ok и :no  выает всегда NO
		 -Ввод в домен исправить
		 -переименование ПК сделть перед перзагрузкой, или убрать. (из за FI), 
		 
	-- FIAgent
	   	 -при переименовании пк, заносит о нем данные как о новой локльной машине.	

_________________________________________________________________________________________
как должно работать:
		исполнение UVNC script1:
				запрос на выполнение праметров исполнения (1)пеименовать,в домен,FIA;2)переименовать,FiA;3)FIA;4)переменовать;5)nтолько UVNC,bginfo)
				выполнение  переименования ПК
				выполнение  UVNC
				выполнение  bginfo
				копирование на пк, установщика FIA
				(Создать)копирование на пк, скрипт2   
				выполнение: занесение в реестр автозапуска скрипт2
----------------------------
				проверить если уч. запись Администратора выкл, и ITS,its не создан то:
				 созание учетки ITS (пробовать через POWER SHELL)
				или								
				переименование учетки Администратор в ITS

-----------------------------

			проще:	
				переименовать(в любом случае) Администратора в ITS, даже если он уже ITS, в любом случае активировать ITS

				занести в реестр: вход после презагрузки под ITS, без запроса пароля,
				выполнение  перезагрузка
		исполнение UVNC script2:
				выполнение  установка FIA (должна от имени админа)
		   		ввод в домен  (по возможности внести в скрипт данные логина и пароля админа из полученных в СКРИПТ1 )
						(может перенести ввод в домен в скрипт1, проверить будет ли он запускаться при первом входе из любого пользователя домена.)
				занести в реестр: удаление входа под ITS без пароля.
				Занести в реестр:исполнение Скрипт3 после перезагрузки
				Занести в реестр:удалить исполнение скрипт2 поесле перезагрузки
				создание батника скрипт3 в соседний дерриктории для удаления каталога установки Скрипт2.	
				перезагрузка   
	 	исполенение UVNC script3:
				занести в реестр:удаление исполнения скрипт3
				удаление каталогов исполнения скрипт2
				удаление самого себя
