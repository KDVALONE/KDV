UVNCINST v0.03.17
		Скрипт1: 
		 	- В ХP не создается правило для FI и IСMPv4
			- В XP не запускается FIA
			- В XP так же проблемы если FIA уже стоит
			- В XP если учетка ITS или its стоит то правило на авто вход не рабоатет.
			- Если ПК в домене функция переименования может не рабоать, проверить.

UVNCINST v0.03.14
		Скрипт1:
			В начале скипт 1 (возможно везеде) не работает ERRORLEVEL, пишет NO хотя служба установлена.
				-(может вообще убрать ОК\NO)
				похоже что добавление в реестр, не работает ERRORLEVEL, не критично.
		      * Bginfo выплывает окно установки требует нажать AGREE для продолжения, скопировать и запустить BGI не рабоатет
				-(попробовать испрвить автоустановку, сначала установить при ресстарте запускать ./bgi)
		      * Rename Local User to its требует нажать любую клавишу для продолжения
				-(убрать pause)
		       * После перезагрузки не входитпод its: неверное имя пользователя и пароль.
				-(сверить с правилом в REG на автозаход, и с тем во что переименовывается Админ)
					-был пробел при добавлении в реестр, исправил
		Скрипт2:
		      * При запуске после перезагрузки пишет: "Режим не является внутренней или внешней коммандой,
			неполной коммандой или пакетный файл. Ошибка не удается найти указанный раздел." через нектрое время закрывается.
			хотя скрипт исполняется. но не весь
					не критично
			 	 
		      * В скрипт 2 не попала функция вызова Скрипт3 (может и рабоатет, а скрипт3 не сработал так как пытлася удалить чтото с защ. флешки)
				-(отредактировать добавив указатели на запись в скрипт2 строк с кодом)
		
			Не вводиться в Домен, параметр wmic где указан имя ПК, не попадает в скрипт,так как символ
			 процент нельзя использовать, придумать как передать туда имя пк,
		Скрипт3:
		       * В скрипт3 после его создания вместо удаления директории FIAinstall пытвается удалить UVNCINST.bat 
				-(поправить код, возможны траблы с переменными, или просо ссылка на старый файл.)
					в скрипт не попадает строка с удалением директории, проверить			
		
		
		








UVNCINST v0.88

 ок -- При установке не берет данные из log файла в дериктории, просто не выполняет их.(не видит log файла ?).
 ОК-- Изза этого устанавливается по умолчанию, не устанавливая службу uvnc_service 
 ок Так же изза этого нельзя перекинуть в корневую директорию файл .ini, так как путь указывается по умолчанию. (решаемо если в батнике указать оригинальный путь.)

	Ок-- BG info:
		*-Запускается BGinfo.exe отображаются все данные.
	 	*-Если скриптом заставить запускать заранее подготовленный bginfo.bgi то команда /Timer:0 не работает
	-- Скрипт:
		*-на вызове CALL :ok и :no  выает всегда NO
		 -Ввод в домен исправить
		 -переименование ПК сделть перед перзагрузкой, или убрать. (из за FI), 
		 
	-- FIAgent
	   	 -при переименовании пк, заносит о нем данные как о новой локльной машине.	

_________________________________________________________________________________________
как должно работать:
		исполнение UVNC script1:
				запрос на выполнение праметров исполнения (1)пеименовать,в домен,FIA;2)переименовать,FiA;3)FIA;4)переменовать;5)nтолько UVNC,bginfo)
				выполнение  переименования ПК
				ввод в домен чатсть1 (занесение в переменную имени и пароля админа)
				открыть удаленный доступ
				включить удаленного ассистента
				создать правила порта 5800/5900
				включить iCMPv4 
				создать директорию %systemdrive%\itstestfolder
				выполнение  UVNC
				выполнение  bginfo
				копирование на пк, установщика FIA,
				(Создать)на пк, скрипт2   
				выполнение: занесение в реестр автозапуска скрипт2
----------------------------
				проверить если уч. запись Администратора выкл, и ITS,its не создан то:
				 созание учетки ITS (пробовать через POWER SHELL)
				или								
				переименование учетки Администратор в ITS

-----------------------------

			проще:	
				переименовать(в любом случае) Администратора в ITS, даже если он уже ITS, в любом случае активировать ITS

				занести в реестр: вход после презагрузки под ITS, без запроса пароля,
				выполнение  перезагрузка
		исполнение UVNC script2:
				выполнение  установка FIA (должна от имени админа)
		   		ввод в домен часть2  (по возможности внести в скрипт данные логина и пароля админа из полученных в СКРИПТ1 )
						(может перенести ввод в домен в скрипт1, проверить будет ли он запускаться при первом входе из любого пользователя домена.)
				занести в реестр: удаление входа под ITS без пароля.
				Занести в реестр:исполнение Скрипт3 после перезагрузки
				Занести в реестр:удалить исполнение скрипт2 поесле перезагрузки
				создание батника скрипт3 в соседний дерриктории для удаления каталога установки Скрипт2.	
				перезагрузка   
	 	исполенение UVNC script3:
				занести в реестр:удаление исполнения скрипт3
				удаление каталогов исполнения скрипт2
				удаление самого себя
